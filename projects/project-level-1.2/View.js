const cardsElements = document.querySelectorAll('.deck .card')


function removeAllSelection()
{
    // delete all 'selected' class value for all elements
}

function handleInit ()
{
    // 'init()' is in 'Logic.js'
    init()

    /*
        ... code
    */

    // add the correct value for each 'cardsElement' to 1 - 4
    // use the varaiable 'cards' in 'Logic.js'
}

function handlePrev ()
{
    removeAllSelection()

    // 'prev()' is in 'Logic.js'
    prev()

    // 'currentPosition' is in 'Logic.js'
    // add 'selected' class value for the current 'cardsElement'
    // ... code 
}

function handleNext ()
{
    removeAllSelection()

    // 'next()' is in 'Logic.js'
    next()

    // 'currentPosition' is in 'Logic.js'
    // add 'selected' class value for the current 'cardsElement'
    // ... code 
}


// create a variable (const) 'btnInit'
// give as value the element with class name '.btn-init'
// create 'click' event listener and call the function 'handleInit()'

// create a variable (const) 'bntPrev'
// give as value the element with class name '.btn-prev'
// create 'click' event listener and call the function 'handlePrev()'

// create a variable (const) 'btnNext'
// give as value the element with class name '.btn-next'
// create 'click' event listener and call the function 'handleNext()'

