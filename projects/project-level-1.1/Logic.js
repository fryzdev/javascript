let cards = []
let currentPosition = 0  // position can be only: 0 to (SIZE - 1)
let SIZE = 4 // cards[0] cards[1] cards[2] cards[3] => poistion: 0 to (SIZE - 1) 


function init ()
{
    // fill the array 'cards' to 1 - 4

    console.log('init ...')
}

function prev ()
{
    // increment the value of 'currentPosition'

    // print the value of 'currentPosition'

    console.log('prev ...')
}

function next ()
{
    // decrement the value of 'currentPosition'

    // print the value of 'currentPosition'

    console.log('next ...')
}

